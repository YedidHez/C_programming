#include #include typedef struct Node *link; /* указатель на звено */ typedef char elemtype; /* тип элемента списка */ typedef struct Node { /* звено состоит из двух полей: */ elemtype elem; /* – элемент списка, */ link next; /* – указатель на следующее звено */ } node; typedef link list; /* список задается указателем на звено */ list lst; /* переменная типа список */

Источник: https://intellect.icu/svyazannye-spiski-na-primerakh-v-yazyke-si-4498
